<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.dnlab.domain.post.repository.PostMapper">

    <!-- 게시글 추가 -->
    <insert id="createPost">
        INSERT INTO POST(TITLE, CONTENT, BOARD_NUM,user_num,created_at)
        VALUES (#{title}, #{content}, #{board_num},#{user_num},#{createdAt})
    </insert>
</mapper>